#include <stdio.h>
#include <stdlib.h>

int main()
{
    int v, n, n_z[1000000]  ;
    scanf("%d", &v);
    scanf("%d", &n);
    for(int i = 0; i < v; ++i){
        int x;
        scanf("%d", &x)
        for(int j = x; x < n; x+=j++)
        {
            n_z[id++] = x;
        }
        
    }
}